<div class="header-margin">
<div class="header-info">
  <h1>Logging</h1>
  <p><b>Enables users to log type of meal, cuisine type, and ingredients in their meal and populates a list of recipes, servings, and nutrition facts which the user can log. The user can also log individual food items instead of choosing from a list of recipes.</b></p>
</div>
</div>

<div class="header bg-primary pb-6">
  <div class="container-fluid">
    <div class="header-body">
      <%= render partial: 'pages/header', locals: {
            icon: 'home',
            breadcrumb_1: 'Logging',
            breadcrumb_2: 'Logging'
      } %>
    </div>
  </div>
</div>
<!-- Page content -->
<div class="container-fluid mt--6">
  <div class="row">
    <div class="col">
     <% if local_assigns.has_key? :status_msg %>
      <p style="color:white;font-weight:bold;"><%= status_msg %></p>
    <% end %>

      <%= render 'pages/tables/foodlog', recipes: local_assigns[:recipes], fields: local_assigns[:feedback]%>
    </div>
  </div>
  <%# <div class="row">
    <div class="col">
      <%= render 'pages/tables/dark' %>
    <%# </div>
  </div> %> 
  <%# <%= yield :header_tags %> 
  <%# <%= yield :customcss %> 
  <%= render 'pages/footer' %>
</div>
