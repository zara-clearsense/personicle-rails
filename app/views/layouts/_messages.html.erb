<div class="row">

<div class="col-xs-10 col-xs-offset-1">

<% flash.each do |name, msg| %>

    <div class='alert alert-<%="#{name}" %> ' >

         <a href="#" class="close" data-dismiss="alert">&times</a>

        <%# <%= content_tag :div, msg, :class => "strong", :id => "flash_#{name}" if msg.is_a?(String) %> 
        <div class="" id="flash_#<%=name%>" >
                <p style="color:white;font-size:15px;text-align:center"><strong><%= msg %></strong></p>
        </div>
                
  
    </div>

<% end %>

</div>

</div>