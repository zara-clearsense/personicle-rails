<div class="card bg-default">
  <div class="card-header bg-transparent">
    <div class="row align-items-center">
      <div class="col">
        <h6 class="text-uppercase text-muted text-white ls-1 mb-1">Performance</h6>
        <h5 class="h3 text-white mb-0">Your Summary</h5>
      </div>
    </div>
  </div>
  <div class="card-body">
    <!-- Chart -->
    <div class="chart">
       <% if user_data.length() != 0 %>
      <%# <%= react_component("TimeLine", { user_data: user_data})%> 
      <%= react_component("BarChart", { userSummary: user_data})  %>
    <%else%>
      <h3 style="color:black;"> No activities for the past 3 months</h3>
   <%end%>
       <%# <% if user_data.length() != 0 %>
         <%# <%= react_component("BarChart", { userSummary: user_data}) %>
      <%# <%else%>
        <%# <h3 style="color:white;">No activities for the past 3 months</h3>
      <%end%>   
      <canvas id="chart-bars" class="chart-canvas"></canvas>
    </div>
  </div>
</div>
