

## Personicle - Ruby on Rails Application
Clone the repo:

`bundle install`

`npm install`

``` rake db:create ```

``` rake db:migrate ```

Start up your server.

`rails s`

Point your browser to http://localhost:3000.

